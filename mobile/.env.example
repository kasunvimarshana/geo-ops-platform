# Backend API URL
# For local development, use your computer's IP address (not localhost) when testing on physical devices
# Example: EXPO_PUBLIC_API_URL=http://192.168.1.100:8000
EXPO_PUBLIC_API_URL=http://localhost:8000

# Storage encryption key (REQUIRED for production)
# Generate a secure random key using: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
# This key is used to encrypt sensitive data stored locally on the device
EXPO_PUBLIC_STORAGE_KEY=
